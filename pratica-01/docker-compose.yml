services:
  api:
    container_name: pratica-01
    build: .
    user: "1000:1000"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    env_file:
      - .env
    command: npm run start:dev

volumes:
  node_modules: